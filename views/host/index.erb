<!DOCTYPE html>
<html>

  <head>
    <title> Welcome our lovely host! </title>
    <link rel="stylesheet" href="../css/stylesheet.css" type="text/css" />
  </head>

  <body>

    <header>

      <a href="/">
        <img src="../images/newlogo.png" class='logo'/> 
      </a>

      <div class="header-right">
        <a class="active" href="/spaces">Spaces</a>
        <a href="/host">Host</a>

        <% if !session[:user_id] %>
          <a href="/users/new">Sign up</a>
          <a href="/sessions/new">Log in</a>
        <% end %>
        
      </div>

    </header>

    <% if session[:user_id] %>
      <div class="user">
        <div class="nav">
          <h2>
            Hi <%= User.find(id: session[:user_id]).username %>, thanks for being a host!
          </h2>

          <div>
            <form action='/sessions/destroy' method='post'>
                <input type='submit' value='Log Out'>
            </form>
          </div>
        </div>
      </div>
    <% end %>

    <main>
    
    <div class='form'>
      <form action='/spaces/new' method='get'>
        <input type='submit' value='Create Space'>
      </form>
    
      <form action='/host/requests' method='get'>
        <input type='submit' value='View Requests' required>
      </form>

              
      <form action='/host/spaces' method='get'>
        <input type='submit' value='Update Availability'>
      </form>
    </div>

    </main>

  </body>

</html>